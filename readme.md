# **Compute entropy using HLL with sdsl**

**Usage:**
 
Clear, make directories and download genome
```bash
make configure
```

Install sdsl

```bash
make install-sdsl
```

Compile

```bash
make 
```
